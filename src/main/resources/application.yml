server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employees
    name: employees
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true